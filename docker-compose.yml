version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8000:8000
    volumes:
      - .:/var/www/html
    environment:
      DB_CONNECTION: mysql
      DB_HOST: 103.81.85.224
      DB_PORT: 3306
      DB_DATABASE: educore
      DB_USERNAME: educore
      DB_PASSWORD: nosleep
    command: php artisan serve --host=0.0.0.0 --port=8000

volumes:
  dbdata:
